const str = ["Java", "С", "Python", "Groovy", "Go", "PHP", "Perl", "Simula", "Zonnon", "Eiffel" ];
let i = 0;
while (i < str.length ) {
  if (str[i].length > 3) {
    console.log(str[i])
    }
    i++;
}